{% extends 'base.html.twig' %}

{% block title %}IssueCategory index{% endblock %}

{% block body %}
    <h1>IssueCategory index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for issue_category in issue_categories %}
            <tr>
                <td>{{ issue_category.id }}</td>
                <td>{{ issue_category.name }}</td>
                <td>
                    <a href="{{ path('app_issue_category_show', {'id': issue_category.id}) }}">show</a>
                    <a href="{{ path('app_issue_category_edit', {'id': issue_category.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_issue_category_new') }}">Create new</a>
{% endblock %}
