{% extends 'base.html.twig' %}

{% block title %}IssueCategory{% endblock %}

{% block body %}
    <h1>IssueCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ issue_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ issue_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_issue_category_index') }}">back to list</a>

    <a href="{{ path('app_issue_category_edit', {'id': issue_category.id}) }}">edit</a>

    {{ include('issue_category/_delete_form.html.twig') }}
{% endblock %}
