{% extends 'base.html.twig' %}

{% block title %}Pepe{% endblock %}

{% block body %}
    <h1>Pepe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pepe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pepe.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ pepe.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pepe_index') }}">back to list</a>

    <a href="{{ path('app_pepe_edit', {'id': pepe.id}) }}">edit</a>

    {{ include('pepe/_delete_form.html.twig') }}
{% endblock %}
