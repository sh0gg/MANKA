{% extends 'app/base.html.twig' %}

{% block title %}Novo equipo — MANKA{% endblock %}

{% block content %}
    <a href="{{ path('app_admin_device_index') }}" class="mb-4.5 inline-flex items-center gap-1.5 text-sm font-medium text-gray-500 hover:text-gray-900">
        <svg class="size-[15px]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 12H5M12 5l-7 7 7 7" stroke-linecap="round" stroke-linejoin="round"/></svg>
        Volver a equipos
    </a>
    <h1 class="mb-5 text-h1 font-bold text-gray-900">Novo equipo</h1>
    <div class="max-w-lg rounded-xl border border-gray-200 bg-white px-5 py-5">
        {{ include('admin/device/_form.html.twig', {form: form, button_label: 'Crear equipo'}) }}
    </div>
{% endblock %}
